<Window x:Class="FLMDesktop.Views.ProductEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Product" Width="440" Height="310"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Name" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBox x:Name="TxtName" Grid.Row="0" Grid.Column="1" Margin="8,6"/>

            <TextBlock Text="Weighted Item" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
            <CheckBox x:Name="ChkWeighted" Grid.Row="1" Grid.Column="1" Margin="8,6" />

            <TextBlock Text="Suggested Price" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBox x:Name="TxtPrice" Grid.Row="2" Grid.Column="1" Margin="8,6"/>
        </Grid>

        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Save" Background="#2563EB" Foreground="White" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
